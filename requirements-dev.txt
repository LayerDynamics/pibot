# Development toolchain for the PiBot Control Suite (host side).
# The runtime package itself has no third-party dependencies in M0; per-milestone
# runtime deps (pyserial, aiohttp, bleak, smbus2, …) are added to a Pi-side
# requirements file when their milestone lands.
hatchling
ruff
mypy
pytest
pytest-cov
# Bundles the pibot.mc sidecar into a standalone binary for the Tauri app's externalBin
# (SPEC-3 / M12.1 T12.1.7). Only needed for `app/scripts/build-sidecar.sh`.
pyinstaller
